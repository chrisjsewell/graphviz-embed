# Use system linker instead of rust-lld on Linux
# This fixes static library linking issues in release mode
[target.x86_64-unknown-linux-gnu]
linker = "cc"

[target.aarch64-unknown-linux-gnu]
linker = "cc"
