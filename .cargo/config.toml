# Use system linker (gcc/ld) instead of rust-lld on Linux
# This fixes static library linking issues in release mode
[target.x86_64-unknown-linux-gnu]
linker = "gcc"

[target.aarch64-unknown-linux-gnu]
linker = "gcc"
